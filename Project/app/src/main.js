import { createApp } from 'vue'; // Import createApp from vue

import App from './App.vue';

// Create the Vue app using createApp
const app = createApp(App);

// Mount the app to the element with id 'app'
app.mount('#app');
